#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    int n; cin >> n;
    vector<int> v;

    for(int i=1; i<=n; i++){
        int a, b;
        cin >> a >> b;
        v.push_back(b);
    }

    sort(v.begin(), v.end());

    int movie = 1;
    for(int i=0; i<n; i++){
        
    }

    return 0;
}